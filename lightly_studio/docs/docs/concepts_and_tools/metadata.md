# Metadata

Metadata stores arbitrary key-value information about each sample defined by the user. You can use metadata to filter and sort your dataset in the UI or through the Python API.

More details on how to work with metadata will be added soon.
