# Lightly Query Monaco Editor Example

A minimal example that wires Monaco Editor to a Langium language server for the Lightly Query Language, running inside a Web Worker.

## What it includes

- Monaco Editor in the browser
- Langium grammar and generated services for `lightly-query`
- A browser language server worker
- Example Lightly Query snippets with diagnostics

## Project structure

```text
editor-example/
├── src/
│   ├── language/
│   │   ├── lightly-query.langium       # Grammar definition
│   │   ├── lightly-query-module.ts     # Langium service creation
│   │   ├── lightly-query-validator.ts  # Custom validation rules
│   │   └── generated/                  # Generated by Langium CLI
│   ├── language-server-worker.ts       # LSP worker entry point
│   └── main.ts                         # Monaco editor setup
├── index.html
├── package.json
└── langium-config.json
```

## Commands

```bash
cd editor-example
npm install
npm run langium:generate
npm run dev
```

## Notes

- The Monaco editor registers only the `lightly-query` language.
- The language server worker creates Lightly Query Langium services and starts the shared server instance.
- Regenerate the files under `src/language/generated/` after grammar changes.
