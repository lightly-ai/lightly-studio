/** Svelte context key under which the point-cloud workspace is registered and read. */
/* v8 ignore next -- a bare constant has no behavior to test; exclude it from coverage. */
export const POINT_CLOUD_WORKSPACE_CONTEXT_KEY = 'point-cloud-workspace';
