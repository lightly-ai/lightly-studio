# Smart select and integration

Owner: root. Status: in progress.

Implement wand toolbar mode, persisted Mask/Box popover, instant positive/Shift-negative points, Enter inference and preview then Enter commit, Escape discard, stale response protection and latency readout. Reuse existing segmentation preview/constraints/overlap handling and one undo entry. Write to the editor's selected source. Integrate other tasks, run static/build checks without adding tests, launch development app and record verification limits.
